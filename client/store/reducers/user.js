
export const user = {
  id:     null,
  name:   "", 
  token:  null
}

export const reducers = (state=user, action) => {
  switch(action.type) {
    default:
      return state;
  }
}

export default reducers;